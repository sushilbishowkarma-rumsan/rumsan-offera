{
  "version": 2,
  "buildCommand": "cd ../.. && pnpm install && pnpm prisma generate --schema=./apps/api/prisma/schema.prisma && turbo build --filter=@app/api",
  "builds": [
    {
      "src": "dist/main.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/main.js"
    }
  ]
}